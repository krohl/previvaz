import boto3


class S3Client:
    def __init__(self):
        self._client = boto3.client("s3")

    def check_key_exists(self, key: str, bucket_name: str) -> bool:
        s3_objects = self._client.list_objects_v2(Bucket=bucket_name, Prefix=key)
        return bool(s3_objects.get("Contents"))
