SQL_CALENDAR = """
SELECT
  date,
  calendar.id_date_type,
  name
FROM calendar
  join date_type on calendar.id_date_type = date_type.id_date_type
WHERE id_calendar_type = %(calendar_type)s
AND (YEAR(date) = %(year)s OR YEAR(date) = %(next_year)s)
"""
