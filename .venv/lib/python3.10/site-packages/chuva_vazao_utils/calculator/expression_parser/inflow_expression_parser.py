from chuva_vazao_utils.calculator.expression_parser.expression_parser import ExpressionParser


class InflowExpressionParser(ExpressionParser):

    def parse(self, expression) -> str:
        expression = self._sub_codposto(expression)
        expression = self._sub_coeff(expression)
        expression = self._sub_hydrogram(expression)

        return expression
