import pytest

from pytest_mock import MockerFixture
from unittest.mock import MagicMock

from chuva_vazao_utils.database_access.query_formatter import QueryFormatter


@pytest.fixture
def formatter():
    """
    instance of QueryFormatter
    """
    return QueryFormatter()


@pytest.fixture
def mock_select(mocker: MockerFixture) -> MagicMock:
    return mocker.patch('chuva_vazao_utils.database_access.base.base_api.TableBase.select')
